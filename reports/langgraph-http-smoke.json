{
  "passed": true,
  "mode": "production-http",
  "live_model": false,
  "checks": [
    "production HTML and security headers; API authentication",
    "ceo: 459 authorized active employees; CSRF enforced",
    "ceo: semantic definitions and compiled LangGraph available; metadata permissions enforced",
    "education, school OR counts, explicit ratio denominator, weekend hours and department hire/departure comparison",
    "ceo: dictionary role enforced; failed-query trace linked through proxy",
    "rd: 168 authorized active employees; CSRF enforced",
    "rd: semantic definitions and compiled LangGraph available; metadata permissions enforced",
    "rd: dictionary role enforced; failed-query trace linked through proxy",
    "employee: 1 authorized active employees; CSRF enforced",
    "employee: semantic definitions and compiled LangGraph available; metadata permissions enforced",
    "employee: dictionary role enforced; failed-query trace linked through proxy",
    "employee cannot access salary aggregate"
  ]
}
