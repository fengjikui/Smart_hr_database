# 手工实操学习记录

配套路线：[从零手工重建](HANDS_ON.md)。这里区分“助手准备完成”和“学员亲手完成”，不把讲过当成做过。

## 2026-09-20：重新开始

### 学习约定

你希望从创建数据库开始亲手操作，之后逐项配置 Superset，最后理解 Agent 的查询流程。每次只教一个小步骤；你的问题、实际结果和踩坑在本文件追加记录。后续你贴出报错时，记录中移除密码、令牌与敏感连接串。

### 助手准备工作（已完成）

1. 停止当前工作台自己的前后端进程，保留数据库、Superset 和模型服务。
2. 备份 HR 业务库、Superset 元数据库、角色、平台密钥、本机凭据和 SQLite 状态。
3. 删除当前 HR 演示的 2 个连接、5 个数据集、6 个业务/反例用户、8 个角色、3 条 RLS、2 个图表、2 个看板；删除 `hr_v2` 和两个只读数据库角色。
4. 清空应用历史/会话，删除过期映射；保留合成数据导入材料及技术管理员。
5. 核对其他课堂的 4 个连接、17 个保留用户、21 个保留角色、3 条 RLS 和 471 条角色权限关联，前后完全一致。技术管理员实际登录成功。
6. 增加手工学习标记，阻止自动建库、自动导入及旧工作台启动。
7. 验证通过：权限入口 24 项测试；5 个自动初始化/启动入口均被拦截，检查后业务库仍不存在；15 个备份文件 SHA-256 全部一致，文档目录数据与源码一致。

### 准备期间的技术问题（助手遇到，并非你的操作错误）

- 首次清理脚本在创建 Superset 应用上下文之前导入模型，出现 `App not initialized yet`。该次未执行删除。调整为先创建应用，再导入模型后完成重置。
- 删除平台用户/数据库不能只删主记录：数据集、图表、看板、RLS 和查询记录有外键依赖。清理按依赖顺序进行；业务账号的可空审计引用置空，保留审计日志。未知非空外键依赖会导致事务失败，而不是随意级联删除其他对象。
- 自动初始化会补齐被删对象，因此仅仅“删库”不够。学习标记阻断该路径，平台恢复启动则使用只启动已有容器的 `resume`。

### 第 1 步（待你操作）

- 教学目标：通过 `psql` 创建 `hr_v2`，理解这是数据库，不是 schema 或表。
- 已提供：终端连接命令及一条 `CREATE DATABASE hr_v2;`。
- 预期结果：返回 `CREATE DATABASE`；连接仍停留在 `postgres`。
- 你的实际结果：待反馈。
- 你的疑问 / 解答：待记录。
- 下一步：确认成功后，切换数据库并核对当前连接，再创建 schema。

## 后续记录模板

每完成一小步，追加：日期、目标、操作位置、实际输入（不含密码）、实际输出、问题与回答、错误原因与修复、对应代码、下一步。只有看到你的实际结果才标记完成。
